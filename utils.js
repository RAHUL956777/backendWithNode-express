const wish = (names) => {
  console.log(`hello there ${names}`);
};

// default export
module.exports = wish;

const num1 = 10;
const num2 = 20;
const add = () => {
  console.log(`The sum of ${num1} + ${num2} is : ${num1 + num2}`);
};

add();
